# Convolve

A calculator to perform convolution operation on matrices.

# How to use

- Install dependencies

  ```sh
  yarn install
  ```

- Run in dev mode

  ```sh
  yarn dev
  ```

- Build the app

  ```sh
  yarn build
  ```